import Form from './components/Form'
import './styles.css'

export default function App() {

  return (
    <>
      <h1>Sign Up</h1>
      {/*
       * TODO Part 2: Add a visual marker/note that lets users
       * know which fields are required (i.e. must be filled out). 
       */}
      <Form />
    </>
  );
}